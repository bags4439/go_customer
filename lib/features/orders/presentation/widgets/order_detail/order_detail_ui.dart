import 'package:flutter/material.dart';

/// Shared literals for order-detail chrome (mobile app bar, etc.).
abstract final class OrderDetailUi {
  static const primary = Color(0xFF378ADD);
  static const textPrimary = Color(0xFF1A1A18);
  static const textMuted = Color(0xFF999999);
  static const border = Color(0xFFE0DFD8);
  static const infoBg = Color(0xFFE6F1FB);
  static const shimmer = Color(0xFFF0EFE9);
}
