/// Firestore `system_settings` keys used by the force-update gate.
class ForceUpdateSettingsKeys {
  ForceUpdateSettingsKeys._();

  static const String iosVersion = 'iosVersion';
  static const String androidVersion = 'androidVersion';
}
